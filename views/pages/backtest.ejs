<div class="mb-6">
  <h1 class="text-2xl lg:text-3xl font-bold mb-1" style="color: var(--text-strong);"><%= t('Backtest') %></h1>
  <p class="text-[14px]" style="color: var(--text-muted);"><%= t('Replay the model against historical bars.') %></p>
</div>
<%- include('../partials/coming-soon', {
  title: t('Strategy backtester'),
  description: t('Pick a signal threshold, time horizon, and universe — see what would have happened.'),
  icon: 'backtest',
  features: [
    t('CAGR, Sharpe, Sortino, max drawdown'),
    t('Walk-forward and bootstrapped confidence intervals'),
    t('Export trade tape as CSV')
  ]
}) %>
